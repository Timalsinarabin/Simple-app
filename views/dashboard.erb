<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">
    <% if session[:sucessMessage] %>
        <div class="message-box absolute bg-green-500 text-white p-4 text-center rounded shadow-md top-20 right-5">
            <%= session.delete(:sucessMessage) %>
        </div>
    <% elsif session[:errorMessage] %>
        <div class="message-box absolute bg-red-500 text-white p-4 text-center rounded shadow-md top-20 right-5">
            <%= session.delete(:errorMessage) %>
        </div>
    <% end %>
    <h1 class="text-2xl font-bold text-center mt-10">Welcome, <%= session[:username] %>!</h1>
    <p class="text-center">Digital library</p>
    <aside class="sidebar box-border border-r-2 bg-purple-100 h-screen p-3 top-0 absolute">
        <div class="aside-header min-w-8">
            <h5 class="title text-2xl p-2">Menu</h5>
            <svg class="side-menu bg-transparent absolute top-4 right-1 w-8 cursor-pointer hover:top-3.5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
            <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" />
            </svg>
        </div>
        <div class="text-center mt-10">
            <div class="nav-viewall cursor-pointer rounded-md hover:bg-purple-200 p-4 m-2">View All Books</div>
            <div class="nav-addbook cursor-pointer rounded-md hover:bg-purple-200 p-4 m-2">Add a book</div>
            <div class="nav-removebook cursor-pointer rounded-md hover:bg-purple-200 p-4 m-2">Remove Book</div>
            <div class="nav-logout cursor-pointer p-2 bg-red-500 rounded-md hover:bg-red-700 text-white font-bold my-4"><a href="/logout">Logout</a></div>
        </div>
    </aside>
    <main class="main-content container ml-20 pt-6">
        <div class="viewall-books">
            <h2 class="text-xl font-bold">All Books</h2>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-2">
                <ul class="mt-4 md:col-span-2 lg:col-span-3">
                    <% @books.each do |book| %>
                        <li class="bg-white list-none p-4 rounded shadow-md hover:shadow-lg transition-shadow m-4">
                            <h3 class="text-lg font-bold"><%= book.bookname %></h3>
                            <p class="text-gray-600">By <%= book.author %></p>
                        </li>
                    <% end %>
                </ul>
                <div class="Pagination flex justify-center col-span-3 mt-4">
                    <% if @current_page > 1 %>
                        <a href="/dashboard?page=<%= @current_page - 1 %>" class="px-3 py-1 bg-gray-300 rounded hover:bg-gray-400 mx-1">Previous</a>
                    <% end %>
                    <% (1..@total_pages).each do |page| %>
                        <a href="/dashboard?page=<%= page %>" class="px-3 py-1 rounded mx-1 <%= page == @current_page ? 'bg-blue-500 text-white' : 'bg-gray-200 hover:bg-gray-300' %>"><%= page %></a>
                    <% end %>
                    <% if @current_page < @total_pages %>
                        <a href="/dashboard?page=<%= @current_page + 1 %>" class="px-3 py-1 bg-gray-300 rounded hover:bg-gray-400 mx-1">Next</a>
                    <% end %>
                </div>
            </div>
        </div>
        <div class="add-book-form hidden flex flex-col">
            <h2 class="text-xl font-bold mb-4">Add a New Book</h2>
            <form action="/add_book" method="post" class="bg-white p-6 rounded shadow-md">
                <label class="block mb-2" for="title">Title:</label>
                <input class="border border-gray-300 rounded px-3 py-2 w-full outline-none focus:ring-2 focus:ring-blue-500" type="text" id="bookname" name="bookname" required>
                <br>
                <label class="block mb-2 mt-4" for="author">Author:</label>
                <input class="border border-gray-300 rounded px-3 py-2 w-full outline-none focus:ring-2 focus:ring-blue-500" type="text" id="author" name="author" required>
                <br>
                <div class="flex justify-center w-full">
                    <button class="bg-green-500 hover:bg-green-700 text-white font-bold py-2 px-4 rounded mt-4" type="submit">Add Book</button>
                </div>
            </form>
        </div>
        <div class="remove-book-form hidden flex flex-col">
            <h2 class="text-xl font-bold mb-4">Remove a Book</h2>
            <form action="/remove_book" method="post" class="bg-white p-6 rounded shadow-md">
                <label class="block mb-2" for="title">Title:</label>
                <input class="border border-gray-300 rounded px-3 py-2 w-full outline-none focus:ring-2 focus:ring-blue-500" type="text" id="bookname" name="bookname">
                <br>
                <div class="flex justify-center w-full">
                    <button class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded mt-4" type="submit">Remove Book</button>
                </div>
            </form>
        </div>
    </main>
    <script src="/js/index.js"></script>
    <script src="/js/menu.js"></script>
</body>
</html>